
service: strika-api
app: strika-app
org: juanesarango

frameworkVersion: '2'
useDotenv: true

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs12.x
  lambdaHashingVersion: 20201221
  apiGateway:
    shouldStartNameWithService: true

functions:
  whoami:
    handler: handler.whoami
    events:
      - http:
          path: whoami
          method: get
